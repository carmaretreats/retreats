---
import CTA from './CTA.astro';
const buttonLabel = "NÄCHSTE RETREATS"
const buttonHref = "#retreats" // o cualquier ID/URL que quieras usar
---

<section class="w-full bg-cream-bg text-dark-brown py-24 px-6 text-center font-sans">
  <!-- Title -->
  <p
    x-data="{ show: false }"
    x-intersect.once="setTimeout(() => show = true, 0)"
    class="font-[PPEditorialOld-Italic] text-4xl md:text-5xl leading-tight mb-8 reveal-section"
    :class="{ 'show': show }"
  >
    Willkommen bei <span class="italic">Carma Retreats</span>
  </p>

  <!-- Paragraph -->
  <p
    x-data="{ show: false }"
    x-intersect.once="setTimeout(() => show = true, 200)" 
    class="font-[PlusJakartaSans] text-lg md:text-xl max-w-3xl mx-auto mb-12 reveal-section"
    :class="{ 'show': show }"
  >
    Unsere Retreats schaffen einen Raum, in dem du ankommst – bei dir, in Verbindung mit anderen und im Hier und Jetzt.
  </p>

  <!-- CTA Button -->
  <div
    x-data="{ show: false }"
    x-intersect.once="setTimeout(() => show = true, 400)"
    class="reveal-section flex justify-center"
    :class="{ 'show': show }"
  >
    <CTA label={buttonLabel} color="cream" border={true} href={buttonHref} />
  </div>
</section>