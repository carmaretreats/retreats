---
import bg from '../assets/images/Terra-Rossa-Papier.png';
--- 
<section
  id="kontakt"
  class="w-full text-white relative py-20 px-6 sm:px-12"
  style={`background-color:#8b3b30; background-image: url('${bg}'); background-size: cover; background-position: center; background-repeat: no-repeat;`}
>
  <div class="max-w-3xl mx-auto relative z-10">
    <header class="mb-10">
      <h2 class="text-4xl sm:text-5xl leading-tight mb-4" style="font-family: var(--font-editorial);">
        Kontakt und Buchung
      </h2>
      <p class="text-lg sm:text-xl text-white/90" style="font-family: var(--font-editorial);">
        Schreib uns eine Nachricht, um jetzt deinen Platz zu buchen. Du hast noch weitere Fragen? Gerne beantworten wir dir diese — füll dazu einfach das Formular aus.
      </p>
    </header>

    <div class="mb-10">
      <h3 class="text-lg mb-1" style="font-family: var(--font-editorial); color: rgba(0,0,0,0.9);">
        E‑Mail
      </h3>
      <p class="mb-6 text-white/90" style="font-family: var(--font-sans);">
        kontakt@mayogi-yoga.de
      </p>
    </div>

    <form class="space-y-8" novalidate>
      <div>
        <label class="block mb-2 text-sm" style="font-family: var(--font-editorial); color:#0b0b0b;">Ihr Name</label>
        <input
          type="text"
          placeholder="Geben Sie Ihren Namen ein"
          class="w-full bg-white text-gray-700 placeholder-gray-400 rounded-2xl py-4 px-6 shadow-md border border-white/60"
          style="font-family: var(--font-sans);"
        />
      </div>

      <div>
        <label class="block mb-2 text-sm" style="font-family: var(--font-editorial); color:#0b0b0b;">Ihre E‑Mail*</label>
        <input
          type="email"
          placeholder="Geben Sie Ihre E‑Mail-Adresse ein"
          class="w-full bg-white text-gray-700 placeholder-gray-400 rounded-2xl py-4 px-6 shadow-md border border-white/60"
          style="font-family: var(--font-sans);"
        />
      </div>

      <div>
        <label class="block mb-2 text-sm" style="font-family: var(--font-editorial); color:#0b0b0b;">Ihre Nachricht*</label>
        <textarea
          rows="6"
          placeholder="Teilen Sie uns Ihre Fragen mit"
          class="w-full bg-white text-gray-700 placeholder-gray-400 rounded-2xl py-4 px-6 shadow-md border border-white/60 resize-none"
          style="font-family: var(--font-sans);"
        ></textarea>
      </div>

      <div>
        <button
          type="button"
          class="inline-block bg-blue-600 hover:bg-blue-700 text-white py-4 px-10 rounded-full shadow-lg"
          style="font-family: var(--font-editorial);"
        >
          Nachricht senden
        </button>
      </div>
    </form>
  </div>

  <!-- subtle overlay to ensure text contrast -->
  <div class="absolute inset-0 bg-black/10 pointer-events-none"></div>
</section>